#include <stdio.h>

#include <stdlib.h>

#include <time.h>

/**
 * main - Prints single digit base numbers of base ten
 * Return: Always 0 (Success)
 */

int main(void)

{
	int s;

	for (s = '0'; s <= '9'; s++)
	putchar(s);
	putchar('\n');

	return (0);

}
